#!/bin/sh
# post-receive hook
. /home/{{ user }}/.ip_addr
git --work-tree={{ src_dir }} --git-dir={{ git_dir }} checkout -f
cd {{ src_dir }}
{{ azk_agent_start_command }}
echo ""; echo "Starting azk. Maybe downloading images be needed. Please be patient."
{{ azk_restart_command }}